// license-header java merge-point
// Generated by andromda-jsf cartridge (controllers\ControllerImpl.java.vsl)
package com.systemsjr.motshelo.loan.web.details;

import com.systemsjr.motshelo.loan.vo.LoanVO;
import java.util.Date;

/**
 * @see com.systemsjr.motshelo.loan.web.details.LoanDetailsController
 */
public class LoanDetailsControllerImpl
    extends LoanDetailsController
{
    /**
     * The serial version UID of this class. Needed for serialization.
     */
    private static final long serialVersionUID = 7036407180856061850L;

    /**
     * @see com.systemsjr.motshelo.loan.web.details.LoanDetailsController#exitInitialiseDetailsScreen(LoanVO loanVO)
     */
    @Override
    public void exitInitialiseDetailsScreen(ExitInitialiseDetailsScreenForm form)
    {
        // populating value with dummy instance
        LoanVO loanVO = new LoanVO();
        loanVO.setId(new Long((long)3355));
        loanVO.setInstanceMember(null);
        loanVO.setAmount(null);
        loanVO.setStartDate(new Date());
        loanVO.setExpectedEndDate(new Date());
        loanVO.setActualEndDate(new Date());
        loanVO.setBalance(null);
        loanVO.setStatus(null);
        loanVO.setMotsheloInstance(null);
        loanVO.setInterests(null);
        form.setLoanVO(loanVO);
    }

    /**
     * @see com.systemsjr.motshelo.loan.web.details.LoanDetailsController#doInitialiseDetailsScreen(LoanVO loanVO)
     */
    @Override
    public void doInitialiseDetailsScreen(DoInitialiseDetailsScreenForm form)
    {
        // populating value with dummy instance
        LoanVO loanVO = new LoanVO();
        loanVO.setId(new Long((long)3355));
        loanVO.setInstanceMember(null);
        loanVO.setAmount(null);
        loanVO.setStartDate(new Date());
        loanVO.setExpectedEndDate(new Date());
        loanVO.setActualEndDate(new Date());
        loanVO.setBalance(null);
        loanVO.setStatus(null);
        loanVO.setMotsheloInstance(null);
        loanVO.setInterests(null);
        form.setLoanVO(loanVO);
    }

    /**
     * @see com.systemsjr.motshelo.loan.web.details.LoanDetailsController#enterInitialiseDetailsScreen(LoanVO loanVO)
     */
    @Override
    public void enterInitialiseDetailsScreen(EnterInitialiseDetailsScreenForm form)
    {
        // populating value with dummy instance
        LoanVO loanVO = new LoanVO();
        loanVO.setId(new Long((long)3355));
        loanVO.setInstanceMember(null);
        loanVO.setAmount(null);
        loanVO.setStartDate(new Date());
        loanVO.setExpectedEndDate(new Date());
        loanVO.setActualEndDate(new Date());
        loanVO.setBalance(null);
        loanVO.setStatus(null);
        loanVO.setMotsheloInstance(null);
        loanVO.setInterests(null);
        form.setLoanVO(loanVO);
    }

    /**
     * @see com.systemsjr.motshelo.loan.web.details.LoanDetailsController#enterLoanEdit(LoanVO loanVO)
     */
    @Override
    public void enterLoanEdit(EnterLoanEditForm form)
    {
        // populating value with dummy instance
        LoanVO loanVO = new LoanVO();
        loanVO.setId(new Long((long)3355));
        loanVO.setInstanceMember(null);
        loanVO.setAmount(null);
        loanVO.setStartDate(new Date());
        loanVO.setExpectedEndDate(new Date());
        loanVO.setActualEndDate(new Date());
        loanVO.setBalance(null);
        loanVO.setStatus(null);
        loanVO.setMotsheloInstance(null);
        loanVO.setInterests(null);
        form.setLoanVO(loanVO);
    }

    /**
     * @see com.systemsjr.motshelo.loan.web.details.LoanDetailsController#doLoanEdit(LoanVO loanVO)
     */
    @Override
    public void doLoanEdit(DoLoanEditForm form)
    {
        // populating value with dummy instance
        LoanVO loanVO = new LoanVO();
        loanVO.setId(new Long((long)3355));
        loanVO.setInstanceMember(null);
        loanVO.setAmount(null);
        loanVO.setStartDate(new Date());
        loanVO.setExpectedEndDate(new Date());
        loanVO.setActualEndDate(new Date());
        loanVO.setBalance(null);
        loanVO.setStatus(null);
        loanVO.setMotsheloInstance(null);
        loanVO.setInterests(null);
        form.setLoanVO(loanVO);
    }

    /**
     * @see com.systemsjr.motshelo.loan.web.details.LoanDetailsController#exitLoanEdit(LoanVO loanVO)
     */
    @Override
    public void exitLoanEdit(ExitLoanEditForm form)
    {
        // populating value with dummy instance
        LoanVO loanVO = new LoanVO();
        loanVO.setId(new Long((long)3355));
        loanVO.setInstanceMember(null);
        loanVO.setAmount(null);
        loanVO.setStartDate(new Date());
        loanVO.setExpectedEndDate(new Date());
        loanVO.setActualEndDate(new Date());
        loanVO.setBalance(null);
        loanVO.setStatus(null);
        loanVO.setMotsheloInstance(null);
        loanVO.setInterests(null);
        form.setLoanVO(loanVO);
    }

}